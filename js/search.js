const input=document.querySelector('#search');const results=document.querySelector('#results');const count=document.querySelector('#count');const clear=document.querySelector('#clear');const chips=[...document.querySelectorAll('[data-category]')];let apps=[];let category='All';const params=new URLSearchParams(location.search);const initial=(params.get('q')||'').trim();const normalize=v=>String(v??'').toLocaleLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').trim();const special=['trending','latest'];const initialNormalized=normalize(initial);if(input)input.value=initial;if(special.includes(initialNormalized))category=initialNormalized==='trending'?'Trending':'Latest';const spin={name:'SpinFlow',category:'SKMOD Lab',version:'Live',size:'Web',rating:5,developer:'SKMOD Labs',downloads:'Interactive',icon:'⟳',description:'Rotation-controlled loader. Spin slowly for a calm progress rate or spin fast to complete the unlock faster.',modFeatures:'spin flow rotation gyro loader motion interactive'};function escapeHtml(v){return String(v??'').replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function matchesCategory(a){if(category==='All')return true;if(category==='Trending')return Number(a.rating||0)>=4.7;if(category==='Latest')return true;return normalize(a.category)===normalize(category)}function matchesQuery(a,q){if(!q||special.includes(normalize(q)))return true;return normalize([a.name,a.category,a.developer,a.version,a.description,a.modFeatures].join(' ')).includes(normalize(q))}function sortApps(list){if(category==='Latest')return [...list].sort((a,b)=>String(b.dateAdded||'').localeCompare(String(a.dateAdded||'')));if(category==='Trending')return [...list].sort((a,b)=>Number(b.rating||0)-Number(a.rating||0));return list}function card(a){const name=escapeHtml(a.name||'Unnamed App'),icon=escapeHtml(a.icon||'◈'),cat=escapeHtml(a.category||'Apps'),version=escapeHtml(a.version||'Latest'),size=escapeHtml(a.size||'—'),rating=escapeHtml(a.rating||'New'),url=a.name==='SpinFlow'?'sensor-test.html':`app-details.html?app=${encodeURIComponent(a.name||'')}`;return `<article class="result-card glass"><div class="app-symbol">${icon}</div><div class="app-copy"><h3>${name}</h3><p>${cat} · ${version} · ${size}</p><span>★ ${rating}</span></div><a href="${url}">${a.name==='SpinFlow'?'Open':'View'}</a></article>`}function syncChips(){chips.forEach(x=>x.classList.toggle('active',x.classList.contains('active')&&x.dataset.category===category));chips.forEach(x=>x.setAttribute('aria-selected',String(x.dataset.category===category)))}function render(){if(!results)return;const q=(input?.value||'').trim(),base=q&&matchesQuery(spin,q)?[spin,...apps]:apps,filtered=sortApps(base.filter(a=>matchesCategory(a)&&matchesQuery(a,q)));results.innerHTML=filtered.length?filtered.map(card).join(''):`<div class="empty glass"><div>⌕</div><h3>No results found</h3><p>Try another app name, developer or category.</p></div>`;if(count)count.textContent=`${filtered.length} result${filtered.length===1?'':'s'}`}async function init(){try{const r=await fetch('../data/apps.json',{cache:'no-store'});if(!r.ok)throw Error(`HTTP ${r.status}`);const data=await r.json();apps=Array.isArray(data)?data:[]}catch(e){console.error('SKMOD search data error:',e);apps=[];if(results)results.innerHTML='<div class="empty glass"><div>⚠</div><h3>Search is temporarily unavailable</h3><p>Please refresh and try again.</p></div>';if(count)count.textContent='0 results';return}syncChips();render()}input?.addEventListener('input',()=>{const q=input.value.trim(),url=new URL(location.href),qNorm=normalize(q);if(special.includes(qNorm))category=qNorm==='trending'?'Trending':'Latest';else if(category==='Trending'||category==='Latest')category='All';if(q)url.searchParams.set('q',q);else url.searchParams.delete('q');history.replaceState({},'',url);syncChips();render()});clear?.addEventListener('click',()=>{if(!input)return;input.value='';category='All';const url=new URL(location.href);url.searchParams.delete('q');history.replaceState({},'',url);syncChips();input.focus();render()});chips.forEach(chip=>chip.addEventListener('click',()=>{chips.forEach(x=>x.classList.remove('active'));chip.classList.add('active');category=chip.dataset.category||'All';const url=new URL(location.href);if(category==='All')url.searchParams.delete('q');else url.searchParams.set('q',category);history.replaceState({},'',url);if(input)input.value=category==='All'?'':category;syncChips();render()}));init();