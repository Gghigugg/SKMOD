(() => {const canvas=document.getElementById('liquid-canvas');if(!canvas)return;const ctx=canvas.getContext('2d');const start=document.getElementById('sensor-start'),stop=document.getElementById('sensor-stop'),status=document.getElementById('sensor-status'),msg=document.getElementById('sensor-message');let dpr=1,w=0,h=0,running=false,last=performance.now(),motion={x:0,y:0},target={x:0,y:0},pointer={x:0,y:0,active:false};const bubbles=[];const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));function resize(){const r=canvas.getBoundingClientRect();dpr=Math.min(2,devicePixelRatio||1);w=r.width;h=r.height;canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);ctx.setTransform(dpr,0,0,dpr,0,0)}function seed(){bubbles.length=0;const count=Math.max(28,Math.min(54,Math.round((w*h)/17000)));for(let i=0;i<count;i++){const rad=14+Math.random()*28;bubbles.push({x:rad+Math.random()*(w-rad*2),y:rad+Math.random()*(h-rad*2),vx:(Math.random()-.5)*.7,vy:(Math.random()-.5)*.7,r:rad,mass:rad*rad,phase:Math.random()*7,stretch:1})}}function setStatus(t){if(status)status.textContent=t}function drawBubble(b){const s=b.stretch,g=ctx.createRadialGradient(b.x-b.r*.35*s,b.y-b.r*.42,1,b.x,b.y,b.r*1.2);g.addColorStop(0,'rgba(255,255,255,.48)');g.addColorStop(.18,'rgba(210,245,255,.25)');g.addColorStop(.55,'rgba(88,184,255,.11)');g.addColorStop(1,'rgba(40,120,210,.025)');ctx.save();ctx.translate(b.x,b.y);ctx.scale(s,1/s);ctx.beginPath();ctx.arc(0,0,b.r,0,Math.PI*2);ctx.fillStyle=g;ctx.shadowColor='rgba(90,200,255,.20)';ctx.shadowBlur=18;ctx.fill();ctx.shadowBlur=0;ctx.strokeStyle='rgba(205,244,255,.25)';ctx.lineWidth=1;ctx.stroke();ctx.beginPath();ctx.arc(-b.r*.28,-b.r*.34,b.r*.2,0,Math.PI*2);ctx.fillStyle='rgba(255,255,255,.25)';ctx.fill();ctx.restore()}function frame(now){const dt=Math.min(.032,(now-last)/1000);last=now;ctx.clearRect(0,0,w,h);const gx=motion.x,gy=motion.y;for(const b of bubbles){b.vx+=(gx*3.1)*dt;b.vy+=(gy*3.1)*dt;b.vx*=Math.pow(.985,dt*60);b.vy*=Math.pow(.985,dt*60);if(pointer.active){const dx=b.x-pointer.x,dy=b.y-pointer.y,d2=dx*dx+dy*dy,range=130*130;if(d2<range&&d2>4){const f=(1-Math.sqrt(d2)/130)*2.7;b.vx+=(dx/Math.sqrt(d2))*f*dt;b.vy+=(dy/Math.sqrt(d2))*f*dt}}b.x+=b.vx*60*dt;b.y+=b.vy*60*dt;b.stretch=clamp(1+Math.min(.12,Math.abs(b.vx+b.vy)*.025),1,1.12);if(b.x<b.r){b.x=b.r;b.vx=Math.abs(b.vx)*.7}if(b.x>w-b.r){b.x=w-b.r;b.vx=-Math.abs(b.vx)*.7}if(b.y<b.r){b.y=b.r;b.vy=Math.abs(b.vy)*.7}if(b.y>h-b.r){b.y=h-b.r;b.vy=-Math.abs(b.vy)*.7}}for(let i=0;i<bubbles.length;i++)for(let j=i+1;j<bubbles.length;j++){const a=bubbles[i],b=bubbles[j],dx=b.x-a.x,dy=b.y-a.y,dist=Math.hypot(dx,dy),min=a.r+b.r;if(dist>0&&dist<min){const nx=dx/dist,ny=dy/dist,over=(min-dist)*.5;a.x-=nx*over;a.y-=ny*over;b.x+=nx*over;b.y+=ny*over;const rel=(b.vx-a.vx)*nx+(b.vy-a.vy)*ny;if(rel<0){const impulse=-rel*.72;a.vx-=nx*impulse;b.vx+=nx*impulse;a.vy-=ny*impulse;b.vy+=ny*impulse}}}for(const b of bubbles)drawBubble(b);requestAnimationFrame(frame)}function motionHandler(e){const a=e.accelerationIncludingGravity;if(!a)return;const angle=screen.orientation?.angle||0;let x=a.x||0,y=a.y||0;if(angle===90){const t=x;x=y;y=-t}else if(angle===180){x=-x;y=-y}else if(angle===270){const t=x;x=-y;y=t}target.x=clamp(x/9.8,-1.8,1.8);target.y=clamp(y/9.8,-1.8,1.8);motion.x+=((target.x)-motion.x)*.14;motion.y+=((target.y)-motion.y)*.14}async function startSensor(){try{if(typeof DeviceMotionEvent!=='undefined'&&typeof DeviceMotionEvent.requestPermission==='function'){const p=await DeviceMotionEvent.requestPermission();if(p!=='granted')throw new Error('Motion permission was not granted.')}window.addEventListener('devicemotion',motionHandler,{passive:true});running=true;start.disabled=true;stop.disabled=false;setStatus('Active');msg.textContent='Sensor active. Tilt the phone, move it, or touch the bubbles for extra liquid force.'}catch(e){setStatus('Permission needed');msg.textContent='Motion sensor permission was not granted. Allow motion access and try again.'}}function stopSensor(){window.removeEventListener('devicemotion',motionHandler);running=false;start.disabled=false;stop.disabled=true;motion.x=motion.y=0;setStatus('Stopped');msg.textContent='Sensor stopped. Touch the bubbles to create a liquid response.'}start?.addEventListener('click',startSensor);stop?.addEventListener('click',stopSensor);canvas.addEventListener('pointerdown',e=>{pointer.active=true;const r=canvas.getBoundingClientRect();pointer.x=e.clientX-r.left;pointer.y=e.clientY-r.top;canvas.setPointerCapture?.(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!pointer.active)return;const r=canvas.getBoundingClientRect();pointer.x=e.clientX-r.left;pointer.y=e.clientY-r.top});canvas.addEventListener('pointerup',()=>{pointer.active=false});canvas.addEventListener('pointercancel',()=>{pointer.active=false});window.addEventListener('resize',()=>{resize();seed()});resize();seed();requestAnimationFrame(frame)})();